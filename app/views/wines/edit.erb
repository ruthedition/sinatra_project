<div class="edit-wine">

  <section class="error"> 
    <% if @wine && @wine.errors.any? %>
    <img src="/images/broken_glass.png">
    <%@wine.errors.full_messages.each do |m|%>
        <p><%= m %></p>
        <% end %>
    <% end %>
  </section>

  <header>
    <h2>Edit <%=@wine.name%></h2>
  </header>

  <section id="edit-wine-form">
    <form action="/wines/<%=@wine.id%>" method="POST">
      <input type="hidden" name="_method" value="patch">

      <label for="name">Bottle Name:</label>
      <input type="text" name="name" value="<%=@wine.name%>" required>

      <br>

      <label for="type">Country: </label>
      <input type="text" name="country" list="country" value="<%=@wine.country.name%>">
        <datalist id="country" name="country" required>
          <option value="<%=@wine.country.name%>"></option>
          <%@countries.each do |country|%>  
            <option value="<%=country.name%>"><%=country.name%></option>
          <% end %>
        </datalist>

      <br>

      <label for="wine_type">Wine Type: </label>
      <input type="text" name="wine_type" list="wines" value="<%=@wine.wine_type %>">
        <datalist id="wines" name="wine_type" required>
          <option value="<%=@wine.wine_type %>"><%= @wine.wine_type %> </option>
          <option value="Riesling"> Riesling</option>
          <option value="Pinot Gris"> Pinot Gris</option>
          <option value="Sauvignon Blanc"> Sauvignon Blanc</option>
          <option value="Chardonnay"> Chardonnay</option>
          <option value="Pinot Noir"> Pinot Noir</option>
          <option value="Zinfandel"> Zinfandel</option>
          <option value="Syrah"> Syrah</option>
          <option value="Cabernet Sauvignon">Cabernet Sauvignon</option>
        </datalist>
  
      <br>
      
      <label for="price">Price:</label>
        <input type="number" name="price" value="<%=@wine.price%>" required>

      <br>

      <label for="year">Year Sealed:</label>
        <%time = Time.now%>
          <input type="number" min="1900" max="<%=time.year%>" name="year" value="<%=@wine.year_sealed%>" required>

      <br>

      <input type="submit" value="Update Wine">

    </form>
  </section>
  
</div>
